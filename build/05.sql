ALTER TABLE `tags`
ADD COLUMN `callback` VARCHAR(20) CHARACTER SET utf8 COLLATE utf8_general_ci NOT NULL;

CREATE TABLE `tasks` (
	`taskID` SMALLINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`data` SMALLINT UNSIGNED NOT NULL,
	`status` TINYINT(1) UNSIGNED NOT NULL DEFAULT 0,
	`success` TINYINT(1) UNSIGNED DEFAULT NULL,
	`response` TEXT CHARACTER SET utf8 COLLATE utf8_general_ci DEFAULT NULL,
	FOREIGN KEY(`data`) REFERENCES main_data(`id`) ON DELETE CASCADE
);

